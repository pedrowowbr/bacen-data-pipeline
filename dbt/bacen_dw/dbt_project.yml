name: "bacen_dw"
version: "1.0.0"
config-version: 2

profile: "bacen_dw"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  bacen_dw:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +schema: marts
